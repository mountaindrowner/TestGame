// Shared UI font stack. Dash Horizon is the display face (see @font-face in
// index.html); monospace catches any glyphs it lacks (symbols, arrows) via the
// browser's per-glyph fallback — in canvas text too.
export const FONT = '"Dash Horizon", ui-monospace, monospace';

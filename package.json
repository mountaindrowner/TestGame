{
  "name": "repentance",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "REPENTANCE — a Dead Cells-inspired roguevania that is a playable metaphor for repentance.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "assets": "python3 tools/gen_all.py",
    "shot": "node tools/screenshot.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
